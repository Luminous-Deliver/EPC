# A single wildcard group. Named agent groups were removed deliberately:
# under the Robots Exclusion Protocol the most specific matching group wins
# and the wildcard group is then ignored entirely for that agent, so the
# per-crawler "Allow: /" blocks were silently exempting GPTBot, ClaudeBot,
# PerplexityBot, Bingbot and others from the /api/ disallow while granting
# them nothing they did not already have.
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://epc.luminousanddeliver.co.uk/sitemap.xml
